"""
Routes du module training.

Les routes seront implémentées lors de la phase de développement.
Convention :
  - Fonctions nommées : <action>_<ressource>  (ex: list_employees, create_leave_request)
  - Renvoient render_template() pour les pages HTML
  - Renvoient jsonify() + code HTTP pour les appels AJAX internes
"""
from . import bp

# Les routes seront ajoutées ici
